# Configuration templates

Templates are JSON files in this directory used by the configuration apply flow.

Legacy `deploymentProfile` keys in templates are ignored; the runtime always uses consolidated deployment (`ApplicationServer` hosts config server and mongos processes).
